import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// App version: 2.1.0 - Progress tracking and lab reset fixes
createRoot(document.getElementById("root")!).render(<App />);
